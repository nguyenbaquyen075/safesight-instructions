# Lộ trình v0.11 — kế hoạch

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Mỗi task = một nhánh = một PR = một issue GitHub.

**Spec:** `docs/superpowers/specs/2026-09-08-v011-features-design.md` (mục G1–G6 là yêu cầu chi tiết của từng task).

## Global Constraints
- Không chạy `npm run dev`/server/Chrome/Docker trên máy vận hành; kiểm bằng `DATABASE_URL=file:./agent-test.db node --import tsx --test --test-concurrency=1 agent/test/*.test.ts` (task đổi schema: `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION="Đồng ý, chạy db push trên agent-test.db" DATABASE_URL=file:./agent-test.db npx prisma db push --accept-data-loss` rồi `npx prisma generate` TRONG worktree của mình trước khi test), `npx tsc --noEmit`, `npx eslint .`, `python3 -m unittest ai-engine/test_*.py`, `python3 -m py_compile`.
- Không thêm dependency npm/pip.
- Định danh, test title, commit tiếng Anh; UI copy, comment, tài liệu tiếng Việt; tài liệu cùng commit với code.
- Schema đổi ở G3, G4 phải sửa cả `prisma/schema.prisma` và `prisma/postgres/schema.prisma` (test `schema-parity`).
- Thứ tự gộp vào `integration/v0.11`: đợt 1 song song từ `main`: G1, G5, G6; đợt 2 tuần tự từ integration (cùng đụng `ViolationDetailModal`/schema): G2 → G4 → G3.
- Mỗi PR theo `docs/github/PR_TEMPLATE.md`, `Closes #<n>`, kết thúc bằng `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.

## Tasks
| Task | Nhánh | Spec | Test tối thiểu | Commit subject |
|---|---|---|---|---|
| T1 | `feat/restricted-zones` | G1 | `ai-engine/test_zones.py` (intrusions + tracker), `agent/test/zones-shape.test.ts` (type) | `feat(zones): restricted and suspended-load zones raise intrusion violations` |
| T2 | `feat/speaker-announce` | G2 | `agent/test/announce.test.ts` | `feat(speaker): automatic loudspeaker announcements from the agent and the violation modal` |
| T3 | `feat/agent-feedback` | G3 | `agent/test/agent-accuracy-shape.test.ts`, read_camera_history feedback | `feat(agent): human feedback on agent verdicts with accuracy stats and dataset export` |
| T4 | `feat/corrective-actions` | G4 | `agent/test/corrective-actions.test.ts` | `feat(violations): corrective actions with assignee, due date and overdue escalation` |
| T5 | `fix/agent-ops-alert-channels` | G5 | `agent/test/ops-alert.test.ts` | `fix(agent): send operational escalations through every configured alert channel` |
| T6 | `chore/engine-requirements` | G6 | `pip install -r --dry-run` không cần; py_compile không đổi | `chore(engine): pin the Python dependencies in requirements.txt` |

Mỗi task: đọc mục spec → TDD (test RED trước) → docs cùng commit → tự review `git diff` → commit → báo cáo `scratchpad/sdd-v011/<task>-report.md` (DONE / DONE_WITH_CONCERNS / BLOCKED, commit, test summary, concerns).
