---
profile: change
scope: 8ac3758-ab4244d
source_kind: git-range
base_sha: 8ac3758161810af4e5afb45a4519c3fb8f226506
head_sha: ab4244d
dirty: false
reviewed_at: 2026-09-08
---

# Review đợt 2 — phạm vi fix đợt 1 (8ac3758..ab4244d)

Chạy sau khi gộp 10 nhánh fix đợt 1. Lens: `security-review` (finder + lọc dương tính giả), `code-review` (8 finder: line-by-line, removed-behavior, cross-file, reuse, simplification, efficiency, conventions, altitude), `ponytail-review`, `review-experience` (không hoàn thành: phiên bị ngắt hai lần và máy quá tải nên không được phép chạy app).

## Coverage

| Lens | Trạng thái | Ghi chú |
|---|---|---|
| security-review | ran | Không có lỗ hổng mới; nhắc `GET` API không có `auth()` (có từ trước) |
| code-review | ran | 8 finder, kết quả gom ở dưới |
| ponytail-review | ran | net −2 dòng (alias `anthropicKey`, nhánh `inputSchema` chết) |
| review-experience | blocked | Cần app chạy; hoãn tới khi máy rảnh |

## Findings → xử lý

| Finding | Mức | Nhánh fix |
|---|---|---|
| 7 route đọc (`violations`, `violations/[id]`, `cameras`, `cameras/[id]`, `sites`, `sites/[id]`, `users`) trả 200 không cần cookie; list không scope theo site; Sidebar tải cả bảng mỗi 15s | high | `fix/api-session-scope` (cd84f7c) |
| `User.role` DB chữ HOA vs enum thường → user tạo qua DB bị coi là bị giới hạn site | medium | `0e9cdc9` |
| Bind mount `./data` không ghi được với uid `app`; thiếu hướng dẫn di chuyển từ named volume | high | `fix/deploy-engine-env` (729fa1e) |
| Engine nuốt 401 từ bridge; `env_local.py` chỉ đọc `.env.local`; `mock_yolo.js` thiếu header; `BRIDGE_PORT` thừa | medium | `fix/deploy-engine-env` |
| `isEngineProcess` false trên non-Linux → phantom `engine.stalled`; heartbeat >5 phút chặn restart engine treo | high | `fix/agent-hardening-2` (8deba0d) |
| Model mặc định `claude-opus-5` gửi tới proxy OpenAI khi Next tạo dòng cài đặt trước worker; PATCH chỉ nhận 2 model Claude | high | `fix/agent-hardening-2` |
| `anthropicClient` bỏ qua `LLM_BASE_URL`; error event gán session của lần thử trước | medium | `fix/agent-hardening-2` |
| Task `ask` reuse race với `retireExhausted` | medium | `fix/agent-hardening-2` |
| Cleanup có thể xoá ảnh bằng chứng của vi phạm còn mở | high | `fix/agent-hardening-2` |
| Probe làm tăng `bridgeFailStreak`/`repeats` | low | `fix/agent-hardening-2` |
| `openai.ts`: nhánh chết, `LlmHttpError` trùng `Anthropic.APIError`, ảnh gửi lặp mỗi vòng | low | `fix/agent-hardening-2` |
| Trạng thái chữ thường cũ: chuẩn hoá một lần thay vì so sánh hai kiểu | low | `fix/agent-hardening-2` |
| Tiêu đề test tiếng Việt (health, openai-client) | convention | `fix/agent-hardening-2` |
| Khối `nextjs-agent-rules` do `next dev` chèn vào AGENTS.md | convention | `chore/conventions-round-2` (4aaeccf): `agentRules: false` |
| Badge Sidebar không trừ "đã đọc" của `/alerts`; trường `badge` chết; helper test trùng | low | `chore/conventions-round-2` + `70dee38` |

## Chưa làm
- Review experience (UI 375/768/1440, ask box, modal) và kiểm tra hình ảnh cho `<input list>` model ở `/agent`, badge Sidebar: chờ máy rảnh để chạy app.
- Refactor `ppe_tracker.py` (god method), phần `localStorage` demo còn lại ở trang Camera: lộ trình.
