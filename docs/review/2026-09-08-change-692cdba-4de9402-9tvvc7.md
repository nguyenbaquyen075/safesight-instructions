---
profile: change
scope: 692cdba-4de9402
source_kind: git-range
base_sha: 692cdba
head_sha: 4de9402
dirty: true
tree_digest: 861a660010270255fc0b3786cd650683a6dd5f20
run_id: 9tvvc7
reviewed_at: 2026-09-08T15:51:56+07:00
---

# Change review — fix wave for the pre-v0.10.0 findings (692cdba..4de9402)

Objective: verify that commit 4de9402 closes the five findings of `2026-09-08-change-3932209-58be986-3cg2si.md` (status-case filter in `list_camera_agents`, orchestration tools leaking into camera-bound asks, `dispatch_to_camera` orphaning a task on a memory-write conflict, untested ask progress logic, missing merge assertion) plus the cheap health items (widget role gate, toaster position, shared `DEMO_CAMERA_IDS`, wiki/04 Zone row) without regressing anything. 14 files.

## Coverage

| Reviewer | Status | Reason / notes |
|---|---|---|
| correctness | ran | Independent subagent: diff, enclosing functions, callers (`preamble.ts`, `session.ts`, `tool-context.ts`, `api/cameras/route.ts`, `use-cameras.ts`, `seed.mjs`, `permissions.ts`) and tests read; `tsc`, `eslint` (10 files) clean; both new test files 6/6 + 6/6. |
| tests | skipped | Small fix commit whose new assertions were reviewed within correctness; full suite executed by the parent (180/180, 0 skipped). No separate tests reviewer dispatched. |
| security-code | skipped | No new trust boundary: tool input schemas unchanged, the widget gate only narrows access (`canAccessPath(role, '/agent')`), no new env/secret/shell/route. |

## Findings

None at blocker/high/medium. Per-key verdict:

- `code/agent-tools/list-camera-agents-status-case-mismatch` — closed (`list_camera_agents.ts:19,27-28`; test asserts `openViolations === 1`, lowercase `status`, default cap 300000).
- `code/agent-toolsets/orchestration-tools-leak-into-subject-bound-sessions` — closed (`toolsets.ts:27` gates on `ctx.cameraId`, same predicate as `preamble.ts:55`; test `names('ask', CAM_A)` excludes both tools).
- `code/agent-tools/dispatch-orphans-task-when-memory-write-conflicts` — closed (`dispatch_to_camera.ts:33-39`, `memoryWritten`; only consumer is the model).
- `tests/agent-hooks/use-ask-session-no-coverage` — closed for the decision logic (`askProgress` in `src/lib/chat-shape.ts`, four boundary cases tested; hook behaviour preserved). The React hook/component rendering itself remains without a runner.
- `tests/camera-orchestration/dispatch-merge-not-asserted` — closed (second dispatch asserts `replacedPending === true` and exactly one pending row).

## Minor observations
- `CameraMonitoringCard` loading-state fallback now shows 6 (true DB-backed demo count) instead of 12; the filtering result is unchanged because `cam-004/006/009–012` never existed as DB rows. The `dispatch_to_camera` try/catch also swallows non-CAS DB errors into `memoryWritten: false` — the behaviour the review asked for.
- UI re-check after the fix (headless Chrome, 1440 and 390): widget visible for the admin, toaster container at top-right (top 80 px) with no overlap on the open panel, no horizontal overflow.
